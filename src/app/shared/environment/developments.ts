export const environment = {

      production: false,
      API_URL: '/api' // Relative path. Dev: `ng serve` proxies to http://localhost:8080 (proxy.conf.json). Prod: Nginx proxies to http://backend:8080 (nginx.conf).

};
